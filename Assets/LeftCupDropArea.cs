using UnityEngine;
/*
//LeftDropArea
using UnityEngine;

public class LeftCupDropArea : MonoBehaviour, ICupDropArea
{
    public void OnCupDrop(Cup cup)
    {
        cup.transform.position = transform.position;

        if (cup.cupState == CupState.Empty)
        {
            cup.ChangeStateTo(CupState.Espresso);
            Debug.Log("Cup changed to espresso.");
            //cup.transform.position = transform.position;
        }
        else if (cup.cupState == CupState.Espresso)
        {
            cup.ChangeStateTo(CupState.Latte);
            Debug.Log("Cup changed to latte.");
            //cup.transform.position = transform.position;
        }
    }
}

*/